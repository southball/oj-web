<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
  <a class="navbar-brand" href="<%= url('') %>"><%= config.site_name %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-body">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbar-body">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%= url('') %>">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= url('contests') %>">Contests</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= url('problems') %>">Problems</a>
      </li>
      <% if (isAdmin) {  %>
      <li class="nav-item">
        <a class="nav-link" href="<%= url('judges') %>">Judges</a>
      </li>
      <% } %>
    </ul>

    <ul class="navbar-nav my-2 my-lg-0">
      <% if (currentUser) { %>
      <li class="navbar-text">
        Welcome, <a href="<%= url('user', currentUser.username) %>"><%= currentUser.username %></a>.
      </li>
      <!-- <% if (isAdmin) { %>
      <li class="nav-item">
        <a class="nav-link" href="<%= url('admin') %>">Admin</a>
      </li>
      <% } %> -->
      <li class="nav-item">
        <a class="nav-link" href="<%= url('auth/logout') %>">Logout</a>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="nav-link" href="<%= url('auth/login') %>">Login / Register</a>
      </li>
      <% } %>
    </ul>
  </div>
</nav>

<!-- Navbar Height Fix -->
<style>
  body>.container {
    padding-top: 80px;
  }
</style>